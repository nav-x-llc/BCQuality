enum 50225 "My Enum"
{
    Extensible = true;

    value(0; "First") { }
    value(1; "Second") { }
    value(2; "Third") { }
    value(3; "NewValue") { } // Appended at the end — no existing ordinal shifts.
}
